spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ims?serverTimezone=UTC&nullCatalogMeansCurrent=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root

  mvc:
    servlet:
      path: /

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
  #      naming:
  #        implicit-strategy:
  thymeleaf:
    cache: false
  resources:
    static-locations: classpath:/static/,classpath:/templates/

#  thymeleaf:
#    prefix: classpath:/templates/
flowable:
  async-executor-activate: false
server:
  port: 8080


